# 計算機異聞 refactor v001 — 2026-08-18

## 今回行ったこと

旧パッケージは一切削除せず、`99_LEGACY_UNTOUCHED/` にそのまま保存した。新構造は旧資料への参照層として追加している。

新しい責務は `CORE → TECH → PREWAR → WARSTART → WARTIME` とし、長大な考察・計算・旧分岐は `REFERENCE` から辿る。

WARSTART は通常の戦史巻戻しの大底。開戦後は checkpoint chain とし、任意の checkpoint から再計算できる。上流変更で WARSTART が変わる場合のみ、旧WARSTART以下を別枝として無効化する。

## ルールを直す場所

`00_CONFIG/refactor_rules_v001.json`。

科学・技術判断と作業ルールを分けており、ルール変更で過去の証拠を削除しない。技術的対立は版番号だけで決めず、条件の同一性、物理・年代制約、一次データ、計算の透明性、後続反証で判定する。決着不能なら両方残して runtime から外す。

## 現在の重要な処理結果

- Pearl state transition は manifest v004 の明示に従い v003 のみを実行対象とし、v001/v002 は保存のみ。
- 旧80と85のMO/MI枝は削除しないが、2026-08-18 handoff の COMINT context dropout を受け、現在の実行checkpointにはしない。シミュレーション停止点は 1942-05-06 24:00。
- 86以降の枝も上流依存のため保存枝とし、再検証なしに再開点へ昇格しない。
- 航空50-53の性能値は47/118/119の監査ゲートを尊重する。特に航空命中率10-25%相対向上は有力な戦史入力だが、52自身の記載通り `provisional_center` とした。
- 暗号70の1942年高級暗号条件は `accepted` としてTECH indexへ明示的に再注入した。

## TECH INDEX v001

速度以外に戦史を変えやすいものを先行抽出した。航空攻撃精度・部隊中央値/可動性、水雷第二斉射、通常潜水艦の稼働/哨戒力、潜高の水中機動、二艦ASW、多層索敵、1942電探、人間介在射撃指揮、高級暗号と残存情報経路、水上艦の予定速力実現性、統合計算規則を含む。

`TECH_INDEX_v001.json` は「全仕様集」ではない。戦史計算時に忘れると結論が変わる差を短く再注入するための第一層であり、詳細値・根拠は旧正本へ降りる。

## 次に行うべき処理

1. TECH index を全主要領域へ拡張する。
2. PREWAR の技術可能性→採用→量産→教育→開戦時実在を項目別に閉じる。
3. WARSTART を新TECH indexの必須領域で監査し、欠欄を埋める。
4. 1942-05-06 checkpoint を再構成し、Coral Seaイベント用handoutを生成してから5/7を再走行する。
